---
name: reviewer
description: Run the reviewer agent on the active session
action: step
agent: reviewer
---

# /reviewer

Runs the reviewer (evaluator) on the active session (code must already
be written). Triggers the correction loop if the review fails.

## Usage

```
@harness /reviewer
```
