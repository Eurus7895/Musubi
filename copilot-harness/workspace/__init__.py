"""Workspace inspection helpers.

harness-tier: substrate
expires-when: never — workspace detection is the per-project context
  the skill router consumes; durable substrate regardless of pipeline
  shape (Track D.1 + D.3).
"""
